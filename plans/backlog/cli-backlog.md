- [ ] Add documentation for bundle:update, bundle:zip, and bundle:unzip commands after successful testing.
- [ ] Fix `bundle:build` (and `cloud:deploy`) failing for Livewire / no-frontend projects: the node-dependency step has no Node pod to run in.
      - Root cause: `runPreDeploymentSteps()` (`app/Traits/InteractsWithRemoteDeploy.php:77-86`) runs `larakube {pm} install`, which proxies through `NpmCommand` → `node` command with `--service=node` (`app/Commands/PackageManager/NpmCommand.php:39`, `app/Commands/NodeCommand.php:43`). For `FrontendStack::LIVEWIRE` (and any stack where `requiresNodePod() === false`, see `app/Enums/FrontendStack.php`) there is **no `node` deployment**, so the exec fails. Same pattern in `EnsuresHostDependencies.php:45` (`runInContainer(..., 'node')`).
      - Proposed fix (do it once, for all stacks): route Node/npm tooling to the **web/PHP pod** instead of a separate `node` pod — the PHP pod is always present and `Dockerfile.php` already needs Node for the `assets` build stage. Either make the `node`/`npm` proxy fall back to the web pod when `! requiresNodePod()`, or always target the web pod for these pre-deploy install steps.
      - Verify first: confirm the runtime web/PHP pod image actually ships `node` + the chosen package manager (it may only exist in the `assets` build stage, not the final `deploy` stage). If not, either add it to the runtime stage or run the install against the build stage — capture the decision in the fix.
- [ ] De-duplicate the "ensure X is configured" prompts shared across `env`, `cloud:configure`, and `cloud:deploy` (keep the three commands separate by altitude — create / configure / ship — but stop re-implementing the same wizards).
      - Hosts (web + reverb + storage): `CloudDeployCommand.php:69-94` (web-only guard) ↔ `configureBase` (web-only) ↔ `EnvCommand` host prompts (full set) ↔ `BundleInstallCommand.php:282-299` (offline host resolution: configured value else `promptForHosts`). Note the drift this already causes: `configureBase` + the deploy guard only handle `web`, while `EnvCommand` handles web+reverb+storage via the shared `promptForHosts()`. The fix unifies everyone on `promptForHosts()` — which is also what `cloud:configure --only=hosts` calls (paas §7.2).
      - Cloud target: `CloudDeployCommand.php:119-123` (`captureCloudConnection`) ↔ `configureBase`.
      - Registry: `EnvCommand::gatherEnvironmentData:169-196` ↔ `configureRegistry`.
      - Extract into shared trait methods (`ensureHosts()` — the full `promptForHosts()` set, not web-only — / `ensureCloudTarget()` / `ensureRegistry()`) so deploy-time guards, configure-time setup, and offline install-time host resolution are the same code and can't drift. Pairs with `plans/active/remove-default-production-env.md` §6 and the `cloud:configure` consolidation (paas-core-expansion §7.2).
- [ ] (Separate, larger refactor) De-duplicate `bundle:install` ↔ `bundle:update` install-side orchestration — ~150 shared lines: preflight tool checks, bundle-dir verification, containerd image-import-with-retry loop, `generateInstallSecrets` + customer `.env` merge + `splitEnvForK8s`, ConfigMap/Secret apply, bundled-kustomize manifest apply, rollout wait. `bundle:update` is effectively `bundle:install` minus swap/k3s-install/CA/TLS/Traefik/hosts/tunnel. Extract the shared flow into a trait (the `GeneratesBundleSecrets` / `GeneratesOfflineCertificates` / `AssemblesBundle` traits already exist; the command-level orchestration is what's still copy-pasted). Note: `BundleUpdateCommand` also imports `GeneratesOfflineCertificates` + `PromptsForHosts` but doesn't appear to use them — confirm and drop dead imports. This is a different axis from the gatherer de-dup above; not part of the `cloud:configure` work.
