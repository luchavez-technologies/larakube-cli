/vendor
/builds
/.idea
/.vscode
/.vagrant
.phpunit.result.cache
.env*
# Generated knowledge graph — ~65MB incl. a cache/ dir, fully rebuildable from
# source. Each contributor generates their own; see .agents/workflows/graphify.md.
/graphify-out
# Nested worktree checkouts used for isolated agent work — never commit a
# full second checkout of this repo into itself.
/.claude/worktrees
